#include <bits/stdc++.h>
using namespace std;
#define REP(i,n) for(int i=0;i<n;i++)
#define endl '\n'

int main() {
  int s; cin >> s;
  cout << (s >= 200 && s <= 299 ? "Success" : "Failure") << endl;
  return 0;
}
