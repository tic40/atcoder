#include <bits/stdc++.h>
using namespace std;
#define REP(i,n) for(int i=0;i<n;i++)
#define endl '\n'

int main() {
  string p; int l; cin >> p >> l;
  cout << ((int)p.size() >= l ? "Yes" : "No") << endl;
  return 0;
}