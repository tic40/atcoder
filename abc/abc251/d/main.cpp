#include <bits/stdc++.h>
using namespace std;
#define REP(i,n) for(int i=0;i<(n);i++)

int main() {
  cout << 99*3 << endl;
  REP(i,99) cout << i+1 << " " << (i+1)*1e2 << " " << (i+1)*1e4 << " ";
  return 0;
}