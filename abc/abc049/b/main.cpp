#include <bits/stdc++.h>
using namespace std;
#define REP(i, n) for(int i = 0; i < n; i++)
typedef long long ll;

int main() {
  int h, w;
  cin >> h >> w;
  string s;
  REP(i,h) {
    cin >> s;
    cout << s << endl << s << endl;
  }
}