#include <bits/stdc++.h>
using namespace std;
#define REP(i,n) for(int i=0;i<n;i++)
#define endl '\n'
using ll = long long;

int main() {
  ll a,b,c; cin >> a >> b >> c;
  cout << (a+b+c == 0 ? "Yes" : "No") << endl;
  return 0;
}