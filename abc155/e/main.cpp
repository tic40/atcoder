#include <bits/stdc++.h>
using namespace std;
#define REP(i, n) for(int i = 0; i < n; i++)
typedef long long ll;

int main() {
  // string s;
  // cin >> s;
  // int S = s.size();

  // if (S==1) {
  //   cout << min( s[0]-'0', 1 + (10 - (s[0]-'0'))) << endl;
  // }

  // ll ans = 0;
  // for(int i = S; i >= 0; i--) {
  //   if (i >= 99) {
  //     ans += (s[i]-'0') * i / 99;
  //   } else ans += min( s[i]-'0', 1 + (10 - (s[i]-'0')));
  // }
  // cout << ans << endl;
}