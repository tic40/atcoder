#include <bits/stdc++.h>
using namespace std;
#define REP(i, n) for(int i = 0; i < n; i++)
typedef long long ll;

int main() {
  double n, m;
  cin >> n >> m;
  cout << ((n / m) == 1 ? "Yes" : "No") << endl;
}